const currentUser = {id:1, username: "mkay0961"}

document.addEventListener("DOMContentLoaded", function() {
  console.log("DOM is loaded");
  let controller = new Controller()
  controller.bootUp()
});
